# Cvičení 2 – `querySelectorAll` + `forEach`

1. Vyber všechny položky seznamu (`<li>`) a do konzole vypiš text každé z nich.
2. Pomocí `forEach` s indexem přepiš text každé položky na tvar: `"1. Mléko"`, `"2. Chléb"` atd.
