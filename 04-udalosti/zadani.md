# Cvičení 4 – Události

1. Po kliknutí na tlačítko zvyš číslo v `<span id="pocet">` o 1.
2. Po stisku klávesy `Šipka nahoru` přidej do `<div id="info">` text `"Šipka nahoru"`. Po stisku `Šipka dolů` text `"Šipka dolů"`.
